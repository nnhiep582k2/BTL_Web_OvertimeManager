namespace OVERTIME.MANAGER.MAIN.Utils.Enums
{
    /// <summary>
    /// Enum lưu giá trị của dropdown
    /// @author nnhiep
    /// </summary>
    public enum SelectedItem
    {
        // Số bản ghi một trang
        pageSize = 0,
        // Trạng thái đơn làm thêm trong main
        status = 1,
        // Nhân viên
        employee = 2,
        // Ca là thêm
        workingshift = 3,
        // Thời điểm làm thêm
        overtime_workingshift = 4,
        // Người duyệt
        approval = 5,
        // Trạng thái đơn làm thêm trong trang thêm
        status_overtime = 6,
        // Vị trí công việc
        jobposition = 7,
        // Đơn vị công tác
        organization = 8,
    }
}
