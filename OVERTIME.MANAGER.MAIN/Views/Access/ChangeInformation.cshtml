@using OVERTIME.MANAGER.MAIN.ViewModels;
@model Employee

@{
    ViewData["Title"] = "Change Information";
    Layout = "~/Views/Shared/_LayoutDefault.cshtml";
    var navigatorActive = new NavigatorActive { navigatorActive = "Index" };
}

@section header {
    <partial name="~/Views/OvertimeManager/_HeaderOM.cshtml" model="navigatorActive" />
}

@section link {
    <link rel="stylesheet" href="../assets/components/overtime_add/css/style.css" />
    <link rel="stylesheet" href="../assets/components/change_pwd/css/style.css" />
    <link rel="stylesheet" href="../assets/components/change_info/css/style.css" />
}

<main class="container-fluid overtime-main">
    <div class="overtime-wrapper py-3">
        <!-- Heading -->
        <div class="overtime-heading d-flex align-items-center justify-content-between pb-3">
            <div class="heading-left">
                <a asp-action="Index" asp-controller="OvertimeManager"><i class="fa-solid fa-arrow-left"></i></a>
                <div class="fs-5 fw-bold heading-title"> Change Information</div>
            </div>
            <div class="btn-group-wrapper">
                <a type="button" class="btn btn-danger btn-add" asp-controller="OvertimeManager" asp-action="Index">
                    <span>Cancel</span>
                </a>
            </div>
        </div>

        <!-- Content -->
        <div class="overtime-container-detail overtime-container-add">
            <form asp-action="ChangeInformation" class="form-change-pwd" method="POST">
                <div class="form-group">
                    <label asp-for="EmployeeName">Employee Name</label>
                    <div class="flex-col">
                        <input asp-for="EmployeeName" class="form-control" />
                        <span asp-validation-for="EmployeeName" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group">
                    <label asp-for="OrganizationName">Organization Name</label>
                    <div class="flex-col">
                        <select asp-for="OrganizationId" class="form-control dropdown-employee" asp-items="ViewBag.Organization"></select>
                        <span asp-validation-for="OrganizationId" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group">
                    <label asp-for="JobPositionName">JobPosition Name</label>
                    <div class="flex-col">
                        <select asp-for="JobPositionId" class="form-control dropdown-employee" asp-items="ViewBag.JobPosition"></select>
                        <span asp-validation-for="JobPositionId" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group">
                    <label asp-for="PhoneNumber">Phone Number</label>
                    <div class="flex-col">
                        <input asp-for="PhoneNumber" class="form-control" />
                        <span asp-validation-for="PhoneNumber" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group">
                    <input type="submit" value="Save change" class="btn btn-primary btn-add btn-change-info" />
                </div>
            </form>
        </div>
    </div>
</main>
